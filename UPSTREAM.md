# 上游来源与授权边界

本项目基于以下上游代码快照复刻并完成项目标识迁移：

- 上游仓库：<https://github.com/iamclancyliang/nanobot-eino>
- 上游提交：`55e0ee8ec6586109a61225193ee2d313a92fb4d9`
- 快照提交时间：2026-05-11 23:54:29 +08:00
- 本地模块：`github.com/actuagent/cclaw`

## 迁移范围

- 保留上游的 Agent、记忆、会话、工具、飞书渠道、Cron、Heartbeat、Skill、MCP 与 Langfuse 能力。
- 将 Cobra CLI 入口迁移为 `cmd/cclaw`，可执行文件名改为 `cclaw`。
- 默认运行目录改为 `~/.cclaw`，环境变量前缀改为 `CCLAW_`。
- 保留对 `nanobot` 与 `openclaw` Skill 元数据键的读取兼容。
- 不导入上游 `.git` 历史。

## 授权风险

拉取上述快照时，上游仓库根目录未提供 `LICENSE` 或其他明确的软件许可证文件。因此，公开可见代码不应被解释为已经获得复制、修改或再分发授权。

在公开发布、商业使用或向第三方分发本项目之前，项目所有者应取得上游作者的明确授权，或确认上游后续发布的许可证适用于本快照。本文件仅记录技术来源，不构成法律意见，也不替代正式授权。
